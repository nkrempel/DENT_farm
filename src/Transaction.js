


class Transaction {
    constructor(
        transType,
        typeId,
        eggCount
    )
    {
        this.transType = transType; 
        this.typeId = typeId;
        this.eggCount = eggCount
    }
}

export default Transaction;