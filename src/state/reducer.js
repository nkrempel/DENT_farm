

const initialState = {
    workers: [],
    orders: [],
    trackerEntries: []
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer;